/*
[C++ 포준 템플릿 라이브러리(STL)와 활용]
Standard Tamplate Library, STL

1. 컨테이너(container) - 템플릿 클래스
	- 데이터를 저장하고 검색하기 위해 담아두는 자료 구조를 구현한 클래스
	- vector, deque, list, set, map, stack, queue

2. 반복자(iterator) - 컨테이너 원소에 대한 포인터
	- 컨테이너의 원소들을 하나씩 순회 접근하기 위해 만들어진 컨테이너 원소에 대한 포인터
	- iterator, const_iterator, reverse_iterator, const_reverse_iterator

3. 알고리즘 - 템플릿 함수
	- 여러 기능을 구현한 템플릿 함수로, 통칭하여 알고리즘이라고 부름.
	- 컨테이너 클래스의 멤버 함수가 아님
	- copy, merge, random, rotate, equal, min, remove, search,
	  find, move, replace, sort, max, partition, reverse, swap

*/